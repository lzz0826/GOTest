slice[i:j]：用于获取切片 slice 中索引从 i 到 j-1 的子切片。如果 i 省略，则表示从切片的开头开始。如果 j 省略，则表示到切片的末尾结束。

slice[:]：用于获取切片 slice 的整个内容。

make([]T, length)：用于创建一个指定长度的切片，但不指定容量。该方法会创建一个长度和容量相同的切片。

make([]T, length, capacity)：用于创建一个指定长度和容量的切片。

append(slice, elements...)：用于向切片 slice 中追加一个或多个元素，并返回新的切片。如果追加的元素个数超过了切片的容量，append 函数会自动调整切片的容量。

copy(dst, src)：将切片 src 中的内容复制到切片 dst 中，并返回复制的元素个数。

len(slice)：返回切片 slice 的长度，即其中元素的个数。

cap(slice)：返回切片 slice 的容量，即底层数组的长度。
