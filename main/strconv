

Atoi(s string) (int, error): 将字符串转换为 int 类型。

Itoa(i int) string: 将 int 类型转换为字符串。

ParseBool(str string) (bool, error): 将字符串转换为 bool 类型。

FormatBool(b bool) string: 将 bool 类型转换为字符串。

ParseInt(s string, base int, bitSize int) (int64, error): 将字符串转换为 int64 类型。

ParseUint(s string, base int, bitSize int) (uint64, error): 将字符串转换为 uint64 类型。

ParseFloat(s string, bitSize int) (float64, error): 将字符串转换为 float64 类型。

FormatInt(i int64, base int) string: 将 int64 类型转换为字符串。

FormatUint(i uint64, base int) string: 将 uint64 类型转换为字符串。

AppendBool(dst []byte, b bool) []byte: 将 bool 类型的值添加到字节切片中。

AppendInt(dst []byte, i int64, base int) []byte: 将 int64 类型的值添加到字节切片中。

AppendUint(dst []byte, i uint64, base int) []byte: 将 uint64 类型的值添加到字节切片中。

AppendFloat(dst []byte, f float64, fmt byte, prec int, bitSize int) []byte: 将 float64 类型的值添加到字节切片中。

Quote(s string) string: 将字符串转换为带双引号的字符串。

QuoteRune(r rune) string: 将 rune 类型转换为带单引号的字符串。

QuoteToASCII(s string) string: 将字符串转换为 ASCII 编码的带双引号的字符串。

QuoteRuneToASCII(r rune) string: 将 rune 类型转换为 ASCII 编码的带单引号的字符串。

Unquote(s string) (string, error): 将带引号的字符串转换为普通字符串。

UnquoteChar(s string, quote byte) (value rune, multibyte bool, tail string, err error): 从带引号的字符串中提取转义字符。

CanBackquote(s string) bool: 检查字符串是否可以被反引号引用。